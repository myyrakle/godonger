pub fn new_api(domain: String, api_path: String, method_name: String) {
    // 1. create domain file if not exists

    // 2. create handler file if not exists

    // 3. create usecase file if not exists

    // 4. add method to usecase interface of domain file

    // 5. add method to usecase file

    // 6. add method to handler file
}
